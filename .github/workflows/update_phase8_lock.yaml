name: Update Phase 8 lockfile

on:
  push:
    branches:
      - phase/08-hardening-l10n-firebase
    paths:
      - pubspec.yaml

permissions:
  contents: write

jobs:
  lockfile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: phase/08-hardening-l10n-firebase
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.4'
          channel: stable
          cache: true
      - run: flutter pub get
      - name: Commit resolved lockfile
        run: |
          if git diff --quiet -- pubspec.lock; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pubspec.lock
          git commit -m "chore(deps): resolve Firebase and localization packages"
          git push
